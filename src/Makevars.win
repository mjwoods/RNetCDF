# Makevars.win can be tested by uploading package to win-builder.r-project.org

PKG_CPPFLAGS = -DHAVE_LIBUDUNITS2
PKG_LIBS = -L${LIB_HDF5}/lib${R_ARCH} -lnetcdf4 -ludunits2 -lexpat -lhdf5_hl -lhdf5 -lz

